server:
  port: 8062

spring:
  config:
    activate:
      on-profile: dev
  cloud:
    discovery:
      client:
        registration:
          eureka:
            enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8060/eureka/